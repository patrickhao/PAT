#include <cctype>
#include <iostream>
#include <string>
#include <map>

using namespace std;
int main() {
    freopen("./sample_in/", "r", stdin);
    int n;
    map<string, int> dict;

    cin >> n;
    while (n--) {
        string temp;
        cin >> temp;
        if (isdigit(temp[0])) {

        } else {

        }
    }
    return 0;
}